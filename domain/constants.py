from pathlib import Path

ARTIFACTS_ROOT = Path("artifacts")
VIDEOS_DIR = ARTIFACTS_ROOT / "videos"
AUDIO_DIR = ARTIFACTS_ROOT / "audio"
TRANSCRIPTS_DIR = ARTIFACTS_ROOT / "transcripts"
LOCALIZED_DIR = ARTIFACTS_ROOT / "localized"
